import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.com.notification_test',
  appName: 'notificationApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
